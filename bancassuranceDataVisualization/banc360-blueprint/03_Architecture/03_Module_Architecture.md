Replace the term modules with apps where appropriate, and explain that:

An App is a user-facing business capability (Executive, Branch, Renewal).
A Component is a reusable UI element (KPI Card, Sidebar, Table).
A Service performs reusable business or infrastructure tasks (Storage, Upload, Config).
The Core manages application lifecycle and orchestration.

This terminology will make the codebase much easier to understand.
